"""Recall MCP server.

A thin MCP wrapper over the memory kernel (Phase 5). Empty for now — no product
logic yet. All database access must go through ``kernel``; this package never
talks SQL directly.
"""
